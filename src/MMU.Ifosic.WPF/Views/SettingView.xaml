<UserControl x:Class="MMU.Ifosic.WPF.Views.SettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MMU.Ifosic.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="10">
        <Label Content="Optical Switch Address"/>
        <TextBox Text="{Binding OpticalSwitchAddress}" />
        <Label Content="Optical Switch Port"/>
        <TextBox Text="{Binding OpticalSwitchPort}" />
        <Label Content="Integrator Unit Patch Id"/>
        <TextBox Text="{Binding IncomingPort}" />
        <Label Content="Fiber Patchs Id"/>
        <TextBox Text="{Binding OutgoingPorts}" />
        <Button Content="Save" Margin="0 10"/>
    </StackPanel>
</UserControl>
